This directory is designed to contain configuration files for each environment.

For example, it provides a space to maintain nginx or apache files

It is recommended to store SSL certificates under `config/environments`
